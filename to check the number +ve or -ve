#include<stdio.h>
int main()
{
int num;
print("enter a number:\n");
scanf("%d,&num);
if(num>0)
printf("%d +ve number \n",num);
else if (num<0)
printf("%d is -ve number:\n",num);
else
printf("0 is neither +ve nor -ve");
return 0;
}
